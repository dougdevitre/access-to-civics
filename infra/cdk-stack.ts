/**
 * CDK stack — stub. See infra/README.md for the resource table and IAM split.
 *
 * Intentionally not implemented yet: infrastructure follows the Phase 0 pilot, not the
 * other way around. Deploying a 50-state pipeline before proving the Draft -> Mirror loop
 * is fun would be the classic mistake.
 */
export {};
